import sharp from "sharp";
export declare function fetchImage(src: string, allowPartial?: boolean): Promise<sharp.Sharp | undefined>;
export declare function calculateImageSize({ maxWidth, maxHeight, originalAspectRatio, specifiedWidth, specifiedHeight, }: {
    maxWidth: number;
    maxHeight: number;
    originalAspectRatio: number;
    specifiedWidth?: number;
    specifiedHeight?: number;
}): {
    width: number;
    height: number;
};
//# sourceMappingURL=image.d.ts.map